# Google Drive API credentials
  # Pode ser:
  # 1) Caminho para o arquivo JSON (ex.: C:\Users\voce\projeto\credentials.json)
  # 2) O conteúdo JSON inline (cole todo o JSON em uma única linha)
  GOOGLE_CREDENTIALS_JSON=path/to/your/credentials.json
  # Chave da API do OpenAI
  OPENAI_API_KEY=your_openai_api_key_here
  # ID da pasta do Google Drive (opcional)
  GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here